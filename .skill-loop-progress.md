# OMP Alien-Artifact Fifty-Pass Ledger

This file is the authoritative progress record for the user-requested fifty full
iterations over dcg's Oh My Pi (`omp`) integration. It intentionally remains in
the repository after the loop so the audit is reproducible.

## Campaign

- Skills: `alien-graveyard` candidate harvest, then `alien-artifact-coding`
  proof/optimization passes, orchestrated with `repeatedly-apply-skill`
- Target: native OMP protocol bridge, install/uninstall lifecycle, path/profile
  semantics, diagnostics, documentation, and release-blocking conformance
- Total required passes: **50**
- Baseline commit: `b361301aa818f1fc97ebb34d1445350e08285de2`
- Started: `2026-08-24T13:04:58Z`
- Current pass: 10 of 50
- Governing constraint: no file deletion and no destructive Git/filesystem
  commands; every numbered pass is delegated serially, reviewed by the primary
  agent, verified proportionally, recorded here, and committed independently

## Baseline and decision model

- Source state: `main`, clean, equal to `origin/main` at campaign start.
- Host state: macOS load average `110.35 81.13 61.93`; local timing and heavy
  compilation are contaminated, so build/test work uses RCH and performance
  claims require a remote-host fingerprint.
- Binary state: the local `target/release/dcg` is a foreign Linux artifact and
  cannot serve as a macOS runtime baseline; a current remote release build was
  queued before optimization.
- Safety states: `{safe, destructive, indeterminate, infrastructure_fault}`.
- Bridge actions: `{allow, block, fail_open_with_diagnostic}`.
- Asymmetric losses (relative units): destructive false allow `1000`;
  indeterminate false allow `500`; safe false block `5`; safe infrastructure
  fail-open `1`; silent infrastructure failure `25`. Existing public policy is
  preserved: dcg decisions `deny`/`ask`/`indeterminate` block, while an absent or
  unrunnable dcg binary fails open visibly.
- Proof ladder: Tier A golden/contract invariants for every pass; Tier B
  property/metamorphic tests for parsers and path state machines; bounded model
  exploration for lifecycle/protocol state machines where it has positive EV.
- Optimization acceptance: one lever per pass, measured hotspot or concrete
  correctness risk, semantic-isomorphism argument, explicit fallback, and no
  material p99 regression. Clever mechanisms that cannot clear those gates are
  rejected rather than shipped.

## Graveyard candidate cards

| Candidate | Entry | Opportunity | Risk / fallback | Disposition |
|---|---|---:|---|---|
| Protocol transition automata | Session types, model checking (`§5.2`, `§6.18`) | 12.5 | State explosion; model only the four-state bridge | Adopt as tests/spec |
| Generated-source translation validation | Verified compilation (`§6.14`) | 10.0 | Brittle text assertions; execute the emitted bridge and retain golden invariants | Adopt |
| Property tests with shrinking | Property testing (`§6.12`) | 12.5 | Random-only false confidence; deterministic seeds plus boundary examples | Adopt selectively |
| Targeted lifecycle faults | LDFI (`§13.13`) | 10.0 | Incomplete lineage; enumerate explicit install/write/read/remove edges | Adopt |
| Minimal failure traces | Hierarchical delta debugging (`§6.15`) | 6.7 | Harness complexity; use only for observed failures | Conditional |
| Deterministic evidence ledger | Hindsight logging (`§3.10`) | 6.3 | Runtime/logging tax; keep evidence in tests and this ledger | Adopt offline |
| Capability-minimal child process | OCap (`§3.4`) | 8.3 | Environment stripping can break supported overrides; preserve documented `DCG_*` surface | Audit/measure |
| Proof-carrying generated artifact | PCC (`§11.9`) | 5.0 | Full formal proof is disproportionate; ownership marker plus executable contract tests | Lightweight form |
| COW/zygote child evaluator | Zygote (`§15.7`) | 1.7 | Whole-stack/Bun portability barrier; direct spawn remains safe fallback | Reject unless profiling overturns |
| Persistent evaluator daemon | Session/transport reuse | 1.9 | New trust boundary, stale policy/config, lifecycle complexity | Reject for this scope |

## Fifty serial missions

| Pass | Mission and proof obligation | Status | Commit / evidence |
|---:|---|---|---|
| 1 | Correct project-extension anchoring to OMP's cwd-only discovery; prove nested Git cwd does not silently install an unloaded file. | complete | `72e6c42`; 7 Rust + 10 targeted Bats green |
| 2 | Make Unix OMP configuration conflicts non-fatal to the overall installer while preserving truthful conflict status; add a full-flow mutation-sensitive test. | complete | `8f22b4b`; 15 focused Bats green |
| 3 | Make robot `--agent omp` apply `[agents.omp]` pack and allowlist policy, not attribution alone; prove positive and negative policy cases. | complete | `db6c649`; 2 robot-boundary tests + targeted clippy green |
| 4 | Propagate each OMP bash call's effective `cwd` into dcg so project config/allowlists are evaluated where the command executes. | complete | `8b94b9a`; generated-source + real child-cwd tests green |
| 5 | Distinguish default profiles from invalid/non-Unicode profiles and fail without writing where OMP itself rejects startup. | complete | `5d3431a`; 5 CLI profile tests + exact unit + targeted clippy green |
| 6 | Resolve the OMP shell-dialect contract on Windows and close any Cmd/PowerShell evasion without overmatching POSIX-only execution. | complete | `f0354a3`, `ae09248`; mutation red + final-byte exact remote green |
| 7 | Model the OMP bridge as a transition table over dcg exit code × parsed decision; prove every cell has an intentional outcome. | complete | `06ad9a6`, `ae671fc`, `a1bcae0`; 35-cell source contract, fmt/diff green; remote gate pending |
| 8 | Verify stdout/stderr framing: only robot JSON drives decisions, diagnostics remain visible, and no channel swap can silently allow a block. | complete | `2b20a2c`, `522f447`, `ba55a22`; mutation red + final source contract/fmt/diff green |
| 9 | Audit OMP `tool_call` structural validation (tool name, input object, command type/emptiness) with adversarial negative fixtures. | complete | `fe2cb7b`; mutation red + structural source contract/fmt/diff green |
| 10 | Build an executable replay corpus/certificate for the generated TypeScript bridge, not merely Rust string-contains assertions. | pending | |
| 11 | Make all OMP installer/uninstaller tests hermetic against ambient profile/config/agent-dir variables so they cannot touch a live extension. | pending | |
| 12 | Implement OMP's stale profile-derived `PI_CODING_AGENT_DIR` suppression while retaining genuine custom overrides. | pending | |
| 13 | Prove `PI_CONFIG_DIR` normalization matches Node path semantics, including leading backslash behavior on POSIX and Windows. | pending | |
| 14 | Make PowerShell profile validation case-sensitive and align empty/invalid precedence with Rust, Unix, and upstream OMP. | pending | |
| 15 | Separate extension ownership from extension health so doctor cannot accept a truncated/stale/disabled marker-only file. | pending | |
| 16 | Make PowerShell ownership-marker matching case-sensitive and preserve near-marker user files. | pending | |
| 17 | Include OMP in Unix uninstaller pre-confirmation inventory so an OMP-only installation is removed and disclosed. | pending | |
| 18 | Remove duplicate project cleanup candidates and prevent cwd-resolution failure from synthesizing a root-level target. | pending | |
| 19 | Enumerate hidden inactive Windows profiles during uninstall while preserving bounded marker-only deletion. | pending | |
| 20 | Make all OMP read/enumerate/delete failures truthful, non-duplicated, and reflected in summary success markers on Unix and Windows. | pending | |
| 21 | Add a bounded parent-side deadline for the generated child evaluator and prove a stuck process cannot hang every OMP bash call. | pending | |
| 22 | Restore persistent decision history for OMP bridge evaluations with correct `agent_type=omp` attribution. | pending | |
| 23 | Require an executable OMP binary for Unix auto-detection and preserve correct Windows semantics. | pending | |
| 24 | Audit atomic write and crash-consistency behavior for generated extensions, including permissions and partial-write failure. | pending | |
| 25 | Prove uninstall enumeration is complete for active/inactive profiles while remaining bounded and preserving unrelated files. | pending | |
| 26 | Measure cold/warm Bun bridge tails and identify whether caching assumptions are real in OMP's extension lifetime. | pending | |
| 27 | Evaluate executable discovery/cache strategies against binary upgrades and `DCG_BIN` override semantics; ship only a safe positive-EV lever. | pending | |
| 28 | Minimize child-process ambient authority/environment if compatible with documented dcg configuration and diagnostics. | pending | |
| 29 | Profile generated-bridge startup file I/O/syscalls and remove redundant work if it clears the 3% practical-gain gate. | pending | |
| 30 | Produce a reproducible latency certificate tied to host/toolchain/source hash and the shipped hook budget. | pending | |
| 31 | Stress command length, Unicode, invalid-surrogate boundaries, and control characters through the full OMP-to-dcg byte path. | pending | |
| 32 | Fault-inject malformed/partial robot JSON and prove exit/decision precedence cannot fail open after a safety verdict. | pending | |
| 33 | Fault-inject all relevant child exit codes and signal-like termination outcomes with exact diagnostics and decisions. | pending | |
| 34 | Exercise child spawn failure, crash, stalled pipe, and timeout/deadline interactions; document bounded vs unbounded seams. | pending | |
| 35 | Audit TOCTOU/binary replacement between install and tool call; preserve explicit operator override while surfacing trust limits. | pending | |
| 36 | Test symlink, path alias, macOS `/private`, and Windows reparse-like ownership/path cases without broadening deletion authority. | pending | |
| 37 | Differentially test Windows drive, UNC, rooted, device-name, and separator cases across Rust and PowerShell resolvers. | pending | |
| 38 | Stress POSIX glob/newline/control characters in profile/config values; ensure no word-splitting or pattern injection. | pending | |
| 39 | Model concurrent install/uninstall interleavings and verify atomicity/ownership invariants under races. | pending | |
| 40 | Run cross-platform metamorphic parity checks over equivalent path/profile states for Rust, Bash, and PowerShell. | pending | |
| 41 | Simplify the generated TypeScript bridge isomorphically, removing only complexity proven redundant by the transition model. | pending | |
| 42 | Audit duplicate path/profile logic for drift; centralize only when coupling cost is lower than cross-language contract tests. | pending | |
| 43 | Mechanically validate documentation examples and claims against generated source, CLI flags, paths, and live harness output. | pending | |
| 44 | Mutation-audit OMP tests for vacuity: each safety assertion must fail under a targeted one-line semantic mutation. | pending | |
| 45 | Add or strengthen bounded property/fuzz coverage for the profile/path state machine with reproducible counterexamples. | pending | |
| 46 | Add model-based lifecycle sequence tests for absent, foreign, owned, refreshed, partially failed, and removed states. | pending | |
| 47 | Produce a deterministic evidence ledger for installer decisions and verify summary claims are derivable from recorded state. | pending | |
| 48 | Review the OMP integration's trusted computing base and reduce ambient authority/unsafe assumptions with explicit residual risks. | pending | |
| 49 | Perform a final cross-seam adversarial rescan of Rust, Bash, PowerShell, generated TS, tests, docs, and CI wiring. | pending | |
| 50 | Synthesize proof, benchmark, and convergence evidence; rerun every release-blocking OMP gate and close only with no unresolved in-scope defect. | pending | |

## Pass record template

Each completed row links to a detailed record appended below with: fresh agent,
scope inspected, concrete findings, change or three-plus zero-change checks,
isomorphism/error/resource argument, verification, residual risk, and commit.

## Pass 1 record: cwd-only project discovery

- Fresh agent: `omp_pass_01_cwd_anchor`.
- Finding: dcg resolved project extensions through the Git root, while upstream
  OMP loads native project extensions only from the exact process cwd and does
  not require Git or walk ancestors.
- Change: Rust install/doctor resolution and Unix uninstallation now use the
  current cwd; CLI help, README, and agent documentation state the same
  contract.
- Mutation-sensitive evidence: nested-Git tests plant an ancestor `.git`; old
  code would write or accept the ancestor extension, while the new assertions
  require the nested cwd and reject ancestor-only doctor coverage. A non-Git
  install case proves removal of the old Git precondition. Unix tests prove an
  ancestor extension is preserved and a cwd extension is removed.
- Isomorphism/error/resource proof: user/profile resolution, marker ownership,
  atomic writes, and decision behavior are unchanged. `current_dir` failures
  propagate; successful path resolution is now a constant-size join instead of
  an ancestor search.
- Verification: remote `cli_e2e omp` slice on `vmi1293453` reported 7 passed,
  0 failed; targeted OMP uninstaller slice reported 10 passed; primary-agent
  rerun of the two new Bats cases reported 2 passed; `cargo fmt --check`,
  `bash -n uninstall.sh`, and `git diff --check` passed.
- Residual: PowerShell project cleanup parity is intentionally deferred to its
  own bounded pass.

## Pass 2 record: nonfatal, truthful Unix configuration conflict

- Fresh agent: `omp_pass_02_installer_conflict`.
- Finding: under `set -e`, the production installer called `configure_omp`
  bare. Its intended `conflict`/`failed` return values therefore terminated the
  installer before the code that renders those exact states.
- Change: the call boundary now explicitly consumes the nonzero status after
  `OMP_STATUS`/`OMP_FAILURE_REASON` have recorded it; the function's result and
  all summary branches remain intact.
- Mutation-sensitive evidence: the new full-installer fixture failed at its
  exit-status assertion with the production patch absent. It now proves the
  exact `install --omp --force` delegation occurred, the final success section
  was reached, the ownership conflict was printed, and the foreign extension
  remained byte-identical.
- Isomorphism/error/resource proof: success and skip transitions are unchanged;
  only `conflict-recorded -> summary-rendered` and `failed-recorded ->
  summary-rendered` replace an unintended shell abort. No new process,
  allocation, retry, or measurable hot-path cost was introduced.
- Verification: primary-agent rerun of all 15 OMP-filtered Bats cases passed;
  `bash -n install.sh`, `shellcheck install.sh`, and `git diff --check` passed.
- Residual: the nearby OpenCode call boundary has similar shape but is outside
  this OMP-only campaign/pass.

## Pass 3 record: agent-specific policy at the robot boundary

- Fresh agents: `omp_pass_03_agent_policy` established the mutation-sensitive
  failing fixture; after that agent was interrupted, the fresh replacement
  `omp_pass_03_policy_repair` completed the bounded implementation and inverse
  cases.
- Finding: `dcg --robot test --agent omp` used the explicit selector only for
  result attribution. Unlike hook mode, it ignored `[agents.omp]`
  `extra_packs`, `disabled_packs`, `additional_allowlist`, and
  `disabled_allowlist` policy.
- Change: agent pack exclusion and layered-allowlist application are canonical
  `Config` operations shared by hook and robot paths. Robot evaluation now
  resolves the selected agent's policy; invocation-local `--with-packs` is
  deliberately re-applied last and remains the caller's explicit override.
- Mutation-sensitive evidence: before the implementation, the real CLI/stdin
  fixture expected OMP's `containers.docker` extra pack to block
  `docker system prune`, but received exit 0. The completed matrix proves the
  extra/disabled pack cases, additional/disabled allowlist cases, OMP-to-Codex
  non-leakage, and CLI-pack precedence. A first green attempt also caught and
  removed two invalid test assertions about allowlist provenance fields that
  are not part of robot JSON; the actual decisions and agent attribution remain
  asserted.
- Isomorphism/error/resource proof: hook-mode behavior now calls the same
  helpers with no policy change; robot mode gains only the previously omitted
  selected-agent transforms. Keyword collection remains after the final pack
  set, core packs remain mandatory, and no process, I/O, retry, or hot-path
  allocation class was added.
- Verification: remote `cargo test --test robot_mode
  test_omp_robot_boundary -- --nocapture` on `vmi1264463` reported 2 passed,
  0 failed; remote `cargo clippy --test robot_mode -- -D warnings` passed;
  `cargo fmt --check` and `git diff --check` passed.
- Residual: per-call cwd, persistent history, and bridge dialect remain bounded
  missions 4, 22, and 6 respectively.

## Pass 4 record: effective Bash cwd propagation

- Fresh agent: `omp_pass_04_call_cwd`.
- Finding: the generated `tool_call` handler ignored both the structured Bash
  `cwd` and OMP's conservative leading-`cd` normalization, so dcg loaded project
  configuration and allowlists from OMP's ambient directory rather than the
  directory in which Bash would execute.
- Change: the extension consumes callback `ctx.cwd`, reuses OMP's current public
  `resolveToCwd` and `extractLeadingCdTarget` exports, applies OMP's precedence
  `structured cwd -> leading cd -> session cwd`, and passes the result through
  Bun's direct-spawn `cwd`. Filesystem-cwd resolution errors return an explicit
  block instead of evaluating under unrelated policy.
- Mutation-sensitive evidence: the intended generated-source test first failed
  specifically because the resolver import/wiring was absent; an earlier
  zero-test `--exact` invocation was rejected as evidence. The real-binary
  fixture holds the command constant while changing only the child cwd: it is
  allowed in the session directory and denied as `containers.docker` in a
  tool directory whose enforcement-only `.dcg.toml` enables that pack.
- Isomorphism/error/resource proof: command bytes, agent attribution, dialect,
  stdio framing, and direct no-shell spawn are unchanged. The new work is two
  exported OMP helper calls and one spawn option per Bash invocation; invalid or
  unrepresentable cwd state blocks with a diagnostic. No filesystem mutation or
  extra subprocess was added.
- Verification: remote unit filter on `vmi1264463` reported 1 passed, 0 failed,
  3708 filtered; remote `test_omp_robot_boundary_uses_execution_cwd_project_policy`
  reported 1 passed, 0 failed, 23 filtered; an installed-package Bun probe
  imported both public helpers and demonstrated relative, root-alias, and
  leading-`cd` outputs; `cargo fmt --check` and `git diff --check` passed.
- Residual: OMP expands internal protocol cwd values later using session state
  unavailable to this callback, so the bridge deliberately blocks those rather
  than approximating. Later extension handlers can also replace tool input
  without replaying earlier handlers; that is an upstream ordering trust
  boundary for pass 48. Full generated-TypeScript execution remains pass 10.

## Pass 5 record: fail-closed profile-state validation

- Fresh agent: `omp_pass_05_profile_validity`.
- Finding: invalid or non-Unicode winning profile values silently collapsed to
  the default profile, allowing `dcg install --omp` to succeed and write an
  extension even though OMP rejects startup. Doctor could also omit its OMP
  check entirely when an invalid legacy `PI_PROFILE` was the only OMP signal.
- Change: profile resolution is now an explicit `Absent` / `Default` / `Named`
  state machine with fallible validation. Absence consults `PI_PROFILE`; empty,
  whitespace, literal `default`, and valid named `OMP_PROFILE` values suppress
  the legacy value; invalid grammar, reserved device names, and non-Unicode
  winning values fail before either project or user/profile installation can
  write. Both doctor renderers surface the same error, and `PI_PROFILE`
  presence participates in OMP-use detection.
- Mutation-sensitive evidence: the pre-fix real-CLI test expected invalid
  `OMP_PROFILE=..` to fail but observed success and exit 0. The final matrix
  proves valid modern profile plus invalid losing legacy, empty/default modern
  profile plus invalid losing legacy, absent modern plus valid legacy, invalid
  modern plus valid legacy, invalid legacy alone, and Unix non-Unicode modern
  input. Every rejection case fences the default, named fallback, explicit
  agent-dir, and project destinations against writes; doctor preserves an
  existing project marker byte-for-byte while reporting the invalid winner.
- Isomorphism/error/resource proof: valid profile paths and the documented
  presence-based precedence are unchanged. Only states OMP's CLI refuses now
  return `InvalidInput`/`InvalidData`; diagnostics name the winning variable
  and escape its value. The state machine adds no subprocess, retry, filesystem
  scan, or hot bridge work.
- Verification: final-byte remote `cli_e2e profile` on `vmi1264463` reported
  5 passed, 0 failed; the exact unit contract reported 1 passed, 3708 filtered;
  remote `cargo clippy --test cli_e2e -- -D warnings` and
  `cargo clippy --lib -- -D warnings` passed; `cargo fmt --check`, targeted UBS,
  and `git diff --check` found no actionable changed-line issue.
- Source precision: current upstream OMP source at
  `989271449953548f1a399789bfc7500003115ebc` defines the replicated grammar and
  presence-based precedence. The local CLI executable reports `omp/18.0.4`,
  while the separately inspectable installed TypeScript packages report
  `17.4.2`; neither version label is substituted for the source/behavior proof.
- Residual: non-Unicode E2E is Unix-only; native Windows case and wide-string
  behavior remain bounded to pass 14. Stale profile-derived override behavior
  remains pass 12, and full all-target gates remain pass 50.

## Pass 6 record: execution-backend shell dialect

- Fresh agent: `omp_pass_06_windows_dialect`.
- Finding: the generated bridge always passed `--dialect posix`. That is exact
  for OMP's ordinary and managed-async embedded Brush routes, including native
  Windows, but not for a synchronous UI-capable `pty: true` call: OMP executes
  that call through its configured external shell, which may be Cmd or
  PowerShell and therefore interprets caret/backtick evasions differently.
- Change: POSIX remains the zero-lookup default. Only the same eligible local
  PTY predicate OMP uses (`pty: true`, not async, UI present, and
  `PI_NO_PTY != 1`) resolves the configured shell and maps OMP's own public
  classifiers to `cmd`, `ps`, or `posix`. Resolution failure returns an
  explicit block. Documentation now describes both the route table and the
  deliberately unresolved ACP terminal case.
- Fresh-eyes correction: a direct `@oh-my-pi/pi-utils/procmgr` import is legal
  under the package's source exports but is not retained by OMP's compiled
  single-binary loader, which excludes root catch-all wildcard subpaths. The
  final bridge imports the retained root `procmgr` namespace instead. The
  `config/settings` import remains on an explicitly retained `config/*`
  surface, while the broad coding-agent root remains type-only.
- Mutation-sensitive evidence: the exact source-contract test failed remotely
  on the hard-coded bridge (1 run, 0 passed) at the missing dynamic import and
  predicate. On final bytes, remote job `j-29990583984455876` on
  `vmi1264463` reported 1 passed, 0 failed, 3709 filtered. An installed
  classifier probe identified Cmd, Windows PowerShell, pwsh, and Bash as the
  expected dialect families. A zero-test red and a later local-fallback
  dialect-matrix attempt were explicitly rejected as evidence.
- Isomorphism/error/resource proof: ordinary, async, non-UI, and `PI_NO_PTY=1`
  commands retain their exact POSIX analysis without a settings lookup. The
  rare external-PTY route adds one cached settings read and two upstream
  classifiers; no shell or extra process is introduced. Unknown shell names
  follow OMP's own POSIX-argument fallback instead of triggering all-dialect
  replay and false positives.
- Verification: final `cargo fmt --check`, `git diff --check`, and status were
  clean. The established evaluator regressions already pin PowerShell/Cmd
  escape positives and POSIX literal negatives; their attempted pass-local RCH
  reruns were not credited because one fell back locally and its remote retry
  was intentionally interrupted after the final exact contract had completed.
- Shared-tree note: a concurrent actor published the implementation as
  `f0354a3` and the compiled-loader correction as `ae09248` while the fresh
  agent was still verifying. The primary agent preserved those commits and did
  not rewrite published history.
- Residual: OMP's public ExtensionContext exposes neither ACP terminal
  capability nor selected Bash backend, and ACP and JSON-RPC both report
  `mode: "rpc"`. Guessing would overmatch POSIX-only JSON-RPC calls, so non-PTY
  RPC stays POSIX-scoped until upstream exposes the actual route. Full generated
  TypeScript execution remains pass 10; the API boundary returns in pass 48.

## Pass 7 record: monotonic child transition automaton

- Fresh agent: `omp_pass_07_transition_table`.
- Finding: the bridge returned immediately on exit 0 or 2 before parsing
  stdout, so a machine-readable `deny`, `ask`, or `indeterminate` verdict on
  either status was silently erased. Child status and data were two competing
  decision channels without an explicit precedence contract.
- Change: generated TypeScript now contains a pure exported classifier and a
  literal 5 outcome x 7 parsed-verdict transition table. The invariant is
  `block iff parsed deny-like verdict or exit 1`. Exit 0/2 otherwise allows;
  spawn failure and other status otherwise remain the documented visible
  infrastructure fail-open. JSON parsing distinguishes empty, malformed,
  allow, deny, ask, indeterminate, and unknown; reason/rule metadata is kept
  only when it is actually a string.
- Fresh-eyes correction: an initial 1 MiB JSON parse cap was removed. Stdout had
  already been fully buffered, so the cap provided no resource bound and could
  instead reclassify a valid large deny as malformed, violating monotonicity.
  A ceremonial spawn-catch classifier call was also removed; the spawn row
  remains an explicit total replay/model specification with its reachability
  documented.
- Mutation-sensitive evidence: the pre-change source witness was the status-only
  `if (exitCode === 0 || exitCode === 2) return;` before JSON parsing. The new
  Rust contract pins every literal cell and rejects that early return, the
  pseudo-cap, missing pure classifier export, and untyped reason/rule fields.
- Isomorphism/error/resource proof: all normal robot pairs retain their prior
  behavior (`0+allow`, `1+deny/ask/indeterminate`, and `2+warn`). Contradictory
  pairs now resolve monotonically toward safety; malformed/empty exit 1 still
  blocks; status-only infrastructure faults still diagnose and fail open. The
  table is fixed module data, adds no process/I/O/retry, and classification is
  one parse plus two constant-key lookups.
- Verification boundary: final `cargo fmt --check` and `git diff --check`
  passed. Strict RCH attempts could not obtain an admissible worker: the first
  offered `[RCH] local` and was interrupted immediately; later attempts exited
  103 for pressure/slot/preflight constraints. Therefore no targeted Rust
  execution or clippy result is claimed for final bytes in this pass. Pass 10
  owns executable generated-TypeScript replay, and pass 50 must clear the
  pending Rust/clippy gates.
- Shared-tree note: concurrent commits published the table (`06ad9a6`), its
  handler integration (`ae671fc`), and the fresh-eyes cap removal (`a1bcae0`)
  while the delegated agent worked. Published history was preserved.
- Residual: stdout/stderr channel framing is pass 8; process deadline/crash
  behavior is passes 21/34; large-command and control-byte behavior is pass 31.

## Pass 8 record: stdout decision authority and stderr visibility

- Fresh agent: `omp_pass_08_stdio_framing`.
- Finding: the bridge drained both child streams concurrently, but returned
  from both allow and block paths before exposing nonempty stderr. Only an
  infrastructure outcome surfaced it, so useful dcg diagnostics disappeared
  during the two normal protocol outcomes.
- Change: stdout remains the sole input to the pure child classifier. Stderr is
  trimmed separately, included exactly once in an infrastructure diagnostic,
  or emitted exactly once through a shared prefixed diagnostic point before
  either a normal allow or block return. The generated source documents these
  streams as distinct capabilities.
- Mutation-sensitive evidence: before the production edit, the focused source
  witness exited 1 because the required shared stderr route did not exist. The
  final contract pins the sole stdout-fed classifier call, proves the pure
  classifier contains no stderr dependency, orders the shared diagnostic before
  both non-infrastructure returns, counts both emission sites, and retains the
  all-blocking exit-1 row. Thus stderr-shaped JSON on exit 0 remains diagnostic
  rather than fabricating a block, while an empty/swapped stdout on exit 1
  cannot silently allow the command.
- Isomorphism/error/resource proof: parsed decisions, status precedence, spawn
  behavior, process count, buffering, and command bytes are unchanged. The only
  added hot-path work is trimming an already-buffered stderr string and a
  conditional; empty stderr remains silent. Nonempty diagnostics become visible
  once without acquiring decision authority.
- Verification boundary: `cargo fmt --check` and `git diff --check` passed on
  final bytes. Three strict RCH attempts each offered a local fallback because
  all workers were inadmissible under pressure/slot/preflight constraints and
  were immediately interrupted. Bounded UBS also did not finish. No Rust test,
  clippy, or UBS green is claimed; passes 10 and 50 retain those gates.
- Shared-tree note: a concurrent actor published the red contract (`2b20a2c`),
  production repair (`522f447`), and generated capability comment (`ba55a22`)
  while the fresh agent worked. The remaining strengthening assertions and this
  record are landed without rewriting that published history.
- Residual: executable stream replay belongs to pass 10; deadline/stalled-pipe
  behavior remains passes 21 and 34.

## Pass 9 record: total tool-call command-shape boundary

- Fresh agent: `omp_pass_09_tool_shape`.
- Finding: the generated callback silently returned for an identifiable Bash
  event whose input was not an object or whose required `command` was missing or
  non-string. OMP's ordinary agent loop validates this schema before emitting
  `tool_call`, but its extension machinery also has non-loop dispatch seams; a
  safety boundary should not interpret malformed Bash input as consent.
- Change: generated TypeScript exports a pure three-state classifier for
  `other-tool`, `invalid-bash`, and valid `bash`. Other tools remain untouched;
  invalid Bash shape returns an explicit block; an empty primitive string stays
  OMP's schema-valid no-op; every nonempty primitive string, including only
  whitespace, proceeds byte-for-byte to dcg.
- Upstream evidence: current OMP `main` at
  `989271449953548f1a399789bfc7500003115ebc` defines Bash `command` as a
  primitive string without a minimum length, validates arguments before the
  agent-loop `beforeToolCall`, and emits the exact lower-case tool name with a
  normalized record input.
- Mutation-sensitive evidence: the pre-production source probe failed because
  the total classifier and explicit malformed-input block were absent. The
  final contract pins object/array/null rejection, exact tool-name isolation,
  required primitive-string validation, the empty-only exemption, lack of any
  trimming exemption, and classification before child spawn. The pure export is
  reserved for pass-10 executable replay.
- Isomorphism/error/resource proof: valid nonempty Bash commands retain their
  bytes and downstream path/dialect/protocol behavior; other tools and empty
  commands retain their prior no-op behavior. Only previously malformed,
  identifiable Bash events change from silent allow to a deterministic block.
  The boundary adds fixed type checks and no I/O, process, allocation class, or
  retry.
- Verification boundary: `cargo fmt --check` and committed `git diff --check`
  passed. Two strict RCH attempts offered local fallback because no worker was
  admissible and were immediately interrupted; bounded UBS did not finish. No
  targeted Rust or UBS green is claimed, and pass 10/50 retains those gates.
- Shared-tree note: a concurrent actor published the complete implementation
  and test as `fe2cb7b` while the fresh agent verified it; published history was
  preserved.
- Residual: this deliberately validates the event/object/required-command seam,
  not every optional Bash field. Other extensions can mutate the shared input;
  non-boolean `pty`/`async` can diverge from guard routing, and `env` can affect
  shell expansion. Passes 28 and 48 own those ambient-authority/composition
  boundaries rather than concealing them in this pass.
